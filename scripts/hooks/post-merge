#!/bin/sh
# --- vexp start ---

# Generated by vexp — recover index after merge
VEXP_CORE=""
if command -v vexp-core > /dev/null 2>&1; then
    VEXP_CORE="vexp-core"
fi
if [ -n "$VEXP_CORE" ]; then
    "$VEXP_CORE" index --merge-recover > /dev/null 2>&1 || true
fi
# --- vexp end ---
