# UX Polish Round 2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Apply AI button styling, reorder the upload screen, improve work items UX, fix prioritisation table, and polish the risks screen across 13 template files.

**Architecture:** Pure template and CSS changes — no new routes, no controller changes, no DB migrations. `.btn-ai` is defined once in `app.css` and applied across all AI-driven buttons. Upload screen is a full section reorder (cut/paste of existing PHP blocks). Prioritisation gets conditional Fibonacci dropdown values. Risks gets heatmap repositioned and a one-line JS scroll fix.

**Tech Stack:** PHP 8.4 templates, vanilla CSS, vanilla JS

---

## File Map

| Action | File | What changes |
|--------|------|--------------|
| MODIFY | `public/assets/css/app.css` | Add `.btn-ai` class + sticky Score column CSS |
| MODIFY | `templates/diagram.php` | `.btn-ai` on 3 AI buttons |
| MODIFY | `templates/work-items.php` | `.btn-ai` on 2 AI buttons + `margin-left: auto` on button group |
| MODIFY | `templates/user-stories.php` | `.btn-ai` on 2 AI buttons |
| MODIFY | `templates/prioritisation.php` | `.btn-ai` on AI Suggest Scores + Fibonacci WSJF dropdowns |
| MODIFY | `templates/governance.php` | `.btn-ai` on Run Detection |
| MODIFY | `templates/sprints.php` | `.btn-ai` on 2 AI buttons |
| MODIFY | `templates/risks.php` | `.btn-ai` on Generate Risks, heatmap above list, deselection fix |
| MODIFY | `templates/partials/risk-row.php` | `.btn-ai` on Generate Mitigation, RPN before title |
| MODIFY | `templates/partials/work-item-row.php` | Fix `.btn-ai` class on Improve with AI |
| MODIFY | `templates/partials/user-story-row.php` | Fix `.btn-ai` class on Improve with AI |
| MODIFY | `templates/partials/work-item-modal.php` | Widen modal to `max-width: 760px` |
| MODIFY | `templates/upload.php` | Full section reorder + `.btn-ai` on AI buttons |

---

## Task 1: Add .btn-ai CSS class and sticky Score column CSS

**Files:**
- Modify: `public/assets/css/app.css`

- [ ] **Step 1: Read the last 10 lines of `public/assets/css/app.css`** to find where to append (after the Logo Branding section added in the previous sprint).

- [ ] **Step 2: Append the following block at the very end of the file**

```css

/* === AI Action Buttons ==================================================== */

.btn-ai {
    background-color: #3b82f6;
    color: #ffffff;
    border: none;
    cursor: pointer;
    font-weight: 600;
}

.btn-ai:hover,
.btn-ai:focus {
    background-color: #2563eb;
    color: #ffffff;
}

/* === Prioritisation — Sticky Score Column ================================= */

#prioritisation-table th:last-child,
#prioritisation-table td:last-child {
    position: sticky;
    right: 0;
    background: #fff;
    z-index: 1;
    box-shadow: -2px 0 4px rgba(0, 0, 0, 0.06);
}
```

- [ ] **Step 3: Commit**

```bash
git add public/assets/css/app.css
git commit -m "feat(ux): add .btn-ai class and sticky prioritisation score column"
```

---

## Task 2: Apply .btn-ai to page-level templates (diagram, work-items, user-stories, prioritisation, governance, sprints)

**Files:**
- Modify: `templates/diagram.php`
- Modify: `templates/work-items.php`
- Modify: `templates/user-stories.php`
- Modify: `templates/prioritisation.php`
- Modify: `templates/governance.php`
- Modify: `templates/sprints.php`

Read each file before editing to confirm exact context.

### diagram.php (3 buttons)

- [ ] **Step 1: "Regenerate" button in page header**

Find:
```php
            <button type="button" id="generate-diagram-btn" class="btn btn-secondary btn-sm" onclick="generateDiagramAjax()">Regenerate</button>
```
Replace with:
```php
            <button type="button" id="generate-diagram-btn" class="btn btn-ai btn-sm" onclick="generateDiagramAjax()">Regenerate</button>
```

- [ ] **Step 2: "Generate Roadmap" button in empty state**

Find:
```php
        <button type="button" id="generate-diagram-btn" class="btn btn-primary btn-lg" onclick="generateDiagramAjax()" style="padding: 0.75rem 2rem; font-size: 1rem;">
            Generate Roadmap
        </button>
```
Replace with:
```php
        <button type="button" id="generate-diagram-btn" class="btn btn-ai btn-lg" onclick="generateDiagramAjax()" style="padding: 0.75rem 2rem; font-size: 1rem;">
            Generate Roadmap
        </button>
```

- [ ] **Step 3: "Generate OKRs (AI)" button**

Find:
```php
                <button type="submit" class="btn btn-primary btn-sm"
                        onclick="return confirm('Generate SMART OKRs for all nodes? This will replace existing OKRs.')">
                    Generate OKRs (AI)
                </button>
```
Replace with:
```php
                <button type="submit" class="btn btn-ai btn-sm"
                        onclick="return confirm('Generate SMART OKRs for all nodes? This will replace existing OKRs.')">
                    Generate OKRs (AI)
                </button>
```

### work-items.php (2 buttons + button group alignment)

- [ ] **Step 4: "Generate Work Items" / "Regenerate Work Items" button**

Find:
```php
                <button type="submit" class="btn btn-primary"
```
(inside the `/app/work-items/generate` form — the only `btn btn-primary` in that form)

Replace with:
```php
                <button type="submit" class="btn btn-ai"
```

- [ ] **Step 5: "Regenerate Sizing" button**

Find:
```php
                <button type="submit" class="btn btn-secondary"
                        onclick="return confirm('Re-estimate sprint sizing for all work items using AI?')">
                    Regenerate Sizing
                </button>
```
Replace with:
```php
                <button type="submit" class="btn btn-ai"
                        onclick="return confirm('Re-estimate sprint sizing for all work items using AI?')">
                    Regenerate Sizing
                </button>
```

- [ ] **Step 6: Right-align button group — add `margin-left: auto`**

Find:
```php
        <div class="flex items-center gap-2" style="flex-wrap: wrap;">
            <form method="POST" action="/app/work-items/generate"
```
Replace with:
```php
        <div class="flex items-center gap-2" style="flex-wrap: wrap; margin-left: auto;">
            <form method="POST" action="/app/work-items/generate"
```

### user-stories.php (2 buttons)

- [ ] **Step 7: "Split to User Stories" button**

Find:
```php
            <button type="submit" class="btn btn-primary btn-sm mt-2" onclick="return document.querySelectorAll('input[name=\'hl_item_ids[]\']:checked').length > 0 || (alert('Select at least one work item.'), false)">
                Split to User Stories
            </button>
```
Replace with:
```php
            <button type="submit" class="btn btn-ai btn-sm mt-2" onclick="return document.querySelectorAll('input[name=\'hl_item_ids[]\']:checked').length > 0 || (alert('Select at least one work item.'), false)">
                Split to User Stories
            </button>
```

- [ ] **Step 8: "Regenerate Sizing" button (export section)**

Find:
```php
                <button type="submit" class="btn btn-secondary btn-sm"
                        onclick="return confirm('Re-estimate story point sizes for all user stories using AI?')">
                    Regenerate Sizing
                </button>
```
Replace with:
```php
                <button type="submit" class="btn btn-ai btn-sm"
                        onclick="return confirm('Re-estimate story point sizes for all user stories using AI?')">
                    Regenerate Sizing
                </button>
```

### prioritisation.php (1 button)

- [ ] **Step 9: "AI Suggest Scores" button**

Find:
```php
            <button type="button" id="ai-suggest-btn" class="btn btn-primary btn-sm ai-suggest-btn" onclick="requestAiBaseline()">
                AI Suggest Scores
            </button>
```
Replace with:
```php
            <button type="button" id="ai-suggest-btn" class="btn btn-ai btn-sm ai-suggest-btn" onclick="requestAiBaseline()">
                AI Suggest Scores
            </button>
```

### governance.php (1 button)

- [ ] **Step 10: "Run Detection" button**

Find:
```php
            <button type="submit" class="btn btn-primary btn-sm">Run Detection</button>
```
Replace with:
```php
            <button type="submit" class="btn btn-ai btn-sm">Run Detection</button>
```

### sprints.php (2 buttons)

- [ ] **Step 11: "Generate Sprints" button**

Find:
```php
                    <button type="submit" class="btn btn-primary">Generate Sprints</button>
```
Replace with:
```php
                    <button type="submit" class="btn btn-ai">Generate Sprints</button>
```

- [ ] **Step 12: "Auto-Fill Sprints" button**

Find:
```php
                    <button type="submit" class="btn btn-primary btn-sm" onclick="return confirm('Auto-fill sprints with backlog stories by priority?')">Auto-Fill Sprints</button>
```
Replace with:
```php
                    <button type="submit" class="btn btn-ai btn-sm" onclick="return confirm('Auto-fill sprints with backlog stories by priority?')">Auto-Fill Sprints</button>
```

- [ ] **Step 13: Syntax check all six files**

```bash
cd /c/Users/James/Scripts/stratflow
docker compose exec php php -l templates/diagram.php
docker compose exec php php -l templates/work-items.php
docker compose exec php php -l templates/user-stories.php
docker compose exec php php -l templates/prioritisation.php
docker compose exec php php -l templates/governance.php
docker compose exec php php -l templates/sprints.php
```

Expected: `No syntax errors detected` for all six.

- [ ] **Step 14: Commit**

```bash
git add templates/diagram.php templates/work-items.php templates/user-stories.php templates/prioritisation.php templates/governance.php templates/sprints.php
git commit -m "feat(ux): apply .btn-ai to AI buttons across diagram, work-items, stories, prio, governance, sprints"
```

---

## Task 3: Apply .btn-ai to risks, risk-row, work-item-row, user-story-row partials + widen edit modal

**Files:**
- Modify: `templates/risks.php`
- Modify: `templates/partials/risk-row.php`
- Modify: `templates/partials/work-item-row.php`
- Modify: `templates/partials/user-story-row.php`
- Modify: `templates/partials/work-item-modal.php`

### risks.php (1 button)

- [ ] **Step 1: "Generate Risks (AI)" button**

Find:
```php
            <button type="submit" class="btn btn-secondary btn-sm" onclick="return confirm('This will use AI to analyse your work items and generate 3-5 project risks. Continue?')">
                Generate Risks (AI)
            </button>
```
Replace with:
```php
            <button type="submit" class="btn btn-ai btn-sm" onclick="return confirm('This will use AI to analyse your work items and generate 3-5 project risks. Continue?')">
                Generate Risks (AI)
            </button>
```

### risk-row.php (1 button)

- [ ] **Step 2: "Generate Mitigation (AI)" button**

Find:
```php
            <button class="btn btn-sm btn-secondary generate-mitigation-btn" data-id="<?= (int) $risk['id'] ?>">Generate Mitigation (AI)</button>
```
Replace with:
```php
            <button class="btn btn-sm btn-ai generate-mitigation-btn" data-id="<?= (int) $risk['id'] ?>">Generate Mitigation (AI)</button>
```

### work-item-row.php (1 button)

Note: the "Improve with AI" button here is missing the `btn` base class — the replacement adds it.

- [ ] **Step 3: "Improve with AI" button**

Find:
```php
            <button type="submit" class="btn-secondary btn-sm">Improve with AI</button>
```
Replace with:
```php
            <button type="submit" class="btn btn-ai btn-sm">Improve with AI</button>
```

### user-story-row.php (1 button)

- [ ] **Step 4: "Improve with AI" button** (same fix as Step 3)

Find:
```php
            <button type="submit" class="btn-secondary btn-sm">Improve with AI</button>
```
Replace with:
```php
            <button type="submit" class="btn btn-ai btn-sm">Improve with AI</button>
```

### work-item-modal.php (modal width)

- [ ] **Step 5: Widen the modal — line 12**

Find:
```php
    <div class="modal">
```
Replace with:
```php
    <div class="modal" style="max-width: 760px;">
```

- [ ] **Step 6: Syntax check all five files**

```bash
cd /c/Users/James/Scripts/stratflow
docker compose exec php php -l templates/risks.php
docker compose exec php php -l templates/partials/risk-row.php
docker compose exec php php -l templates/partials/work-item-row.php
docker compose exec php php -l templates/partials/user-story-row.php
docker compose exec php php -l templates/partials/work-item-modal.php
```

Expected: `No syntax errors detected` for all five.

- [ ] **Step 7: Commit**

```bash
git add templates/risks.php templates/partials/risk-row.php templates/partials/work-item-row.php templates/partials/user-story-row.php templates/partials/work-item-modal.php
git commit -m "feat(ux): apply .btn-ai to risks/row partials, fix Improve with AI class, widen work-item modal"
```

---

## Task 4: Reorder upload screen (upload → failure → generate → summary → documents)

**Files:**
- Modify: `templates/upload.php`

This task rewrites the section order of `upload.php`. The PHP logic at the top, the page header, and the previous-documents list are unchanged — only the order of the four conditional sections changes.

- [ ] **Step 1: Read `templates/upload.php`** in full to confirm the current content before rewriting.

- [ ] **Step 2: Rewrite `templates/upload.php`** with the new section order shown below.

The complete new file content:

```php
<?php
/**
 * Document Upload Template
 *
 * Clean, guided flow: upload → extract → summarise → next step.
 * Shows the right action at each stage.
 */
$hasDocuments = !empty($documents);
$latestDoc    = $hasDocuments ? $documents[0] : null;
$hasSummary   = $latestDoc && !empty($latestDoc['ai_summary']);
$hasText      = $latestDoc && !empty($latestDoc['extracted_text']);

function formatFileSize(int $bytes): string {
    if ($bytes === 0) return '—';
    if ($bytes < 1024) return $bytes . ' B';
    if ($bytes < 1048576) return round($bytes / 1024, 1) . ' KB';
    return round($bytes / 1048576, 1) . ' MB';
}
?>

<div class="page-header flex justify-between items-center">
    <h1 class="page-title">
        <?= htmlspecialchars($project['name']) ?> &mdash; Document Upload
        <span class="page-info" tabindex="0" role="button" aria-label="About this page">
            <span class="page-info-btn" aria-hidden="true">i</span>
            <span class="page-info-popover" role="tooltip">Upload your strategy documents (PDF, DOCX, PPTX, TXT) or paste text directly. StratFlow extracts the content and generates an AI summary — the foundation for your entire roadmap.</span>
        </span>
    </h1>
    <?php if ($hasSummary): ?>
        <a href="/app/diagram?project_id=<?= (int) $project['id'] ?>" class="btn btn-primary btn-sm">
            Continue to Strategy Roadmap &rarr;
        </a>
    <?php endif; ?>
</div>

<!-- ===========================
     1. Upload Section (always first)
     =========================== -->
<section class="card mb-6">
    <div class="card-header">
        <h2 class="card-title"><?= $hasDocuments ? 'Upload Another Document' : 'Upload Strategy Document' ?></h2>
    </div>
    <form method="POST" action="/app/upload" enctype="multipart/form-data" id="upload-form"
          data-loading="Uploading..." data-overlay="Uploading and extracting text from your document.">
        <input type="hidden" name="_csrf_token" value="<?= htmlspecialchars($csrf_token) ?>">
        <input type="hidden" name="MAX_FILE_SIZE" value="52428800">
        <input type="hidden" name="project_id" value="<?= (int) $project['id'] ?>">

        <div class="drop-zone" id="drop-zone">
            <div class="drop-zone-icon">&#128196;</div>
            <p class="drop-zone-label">Drop file here or click to browse</p>
            <p class="drop-zone-hint">PDF, DOCX, PPTX, XLSX, TXT, CSV, RTF, MP4, MOV, AVI, WEBM, MKV, MP3, M4A, WAV, OGG, AAC &mdash; Max 50 MB for documents &middot; 200 MB for video/audio</p>
            <input type="file" name="document" id="file-input" accept=".pdf,.doc,.docx,.txt,.csv,.md,.rtf,.pptx,.xlsx,.mp4,.mov,.avi,.webm,.mkv,.mp3,.m4a,.wav,.ogg,.aac" class="file-input-hidden">
        </div>
        <div class="selected-file" id="selected-file" style="display:none;">
            <span id="selected-file-name"></span>
            <button type="button" class="btn btn-sm btn-secondary" id="clear-file">Remove</button>
        </div>

        <div class="upload-divider"><span>or paste text directly</span></div>

        <div class="form-group">
            <textarea name="paste_text" id="paste-text" class="form-input" rows="4"
                      placeholder="Paste meeting notes, strategy briefs, or any strategic content here..."></textarea>
        </div>

        <div style="padding: 0 1.5rem 1.5rem; display: flex; justify-content: flex-end;">
            <button type="submit" class="btn btn-primary">Upload &amp; Extract</button>
        </div>
    </form>
</section>

<!-- ===========================
     2. Extraction Failure (contextual — shown below upload)
     =========================== -->
<?php if ($latestDoc && empty($latestDoc['extracted_text']) && !$hasSummary): ?>
<section class="card mb-6" style="border-left: 4px solid var(--danger, #dc2626);">
    <div class="card-body" style="padding:1.25rem 1.5rem;">
        <div style="display:flex; align-items:start; gap:1rem;">
            <div style="flex-shrink:0; width:40px; height:40px; border-radius:50%; background:#fee2e2; display:flex; align-items:center; justify-content:center;">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#dc2626" stroke-width="2.5">
                    <circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><line x1="12" y1="16" x2="12.01" y2="16"/>
                </svg>
            </div>
            <div style="flex:1;">
                <h3 style="margin:0 0 0.35rem; color:#991b1b;">Could not extract text from this document</h3>
                <p class="text-muted" style="margin:0 0 1rem; font-size:0.875rem;">
                    We couldn't read <strong><?= htmlspecialchars($latestDoc['original_name']) ?></strong>. This usually happens with scanned PDFs, image-heavy documents, or encrypted files.
                </p>
                <div style="display:flex; flex-direction:column; gap:0.5rem; font-size:0.875rem;">
                    <div style="display:flex; align-items:center; gap:0.5rem;">
                        <span style="color:var(--primary); font-weight:600;">1.</span>
                        <span>Upload a different format — <strong>DOCX works best</strong></span>
                    </div>
                    <div style="display:flex; align-items:center; gap:0.5rem;">
                        <span style="color:var(--primary); font-weight:600;">2.</span>
                        <span>Paste the text directly into the form above</span>
                    </div>
                    <div style="display:flex; align-items:center; gap:0.5rem;">
                        <span style="color:var(--primary); font-weight:600;">3.</span>
                        <span>Try a different version of the PDF (re-saved or re-exported)</span>
                    </div>
                </div>
                <div style="margin-top:1rem;">
                    <button type="button" class="btn btn-primary btn-sm" onclick="document.getElementById('paste-text').focus(); document.getElementById('paste-text').scrollIntoView({behavior:'smooth',block:'center'});">
                        Paste Text Instead
                    </button>
                </div>
            </div>
        </div>
    </div>
</section>
<?php endif; ?>

<!-- ===========================
     3. Generate AI Summary (below upload — shown when text extracted, no summary yet)
     =========================== -->
<?php if ($hasText && !$hasSummary): ?>
<section class="card mb-6" style="border-left: 4px solid var(--primary);">
    <div class="card-body" style="display:flex; align-items:center; justify-content:space-between; padding:1.25rem 1.5rem;">
        <div>
            <h3 style="margin:0 0 0.25rem;">Document uploaded successfully</h3>
            <p class="text-muted" style="margin:0; font-size:0.875rem;">
                Text extracted from <strong><?= htmlspecialchars($latestDoc['original_name']) ?></strong>.
                Generate an AI summary to proceed to the strategy roadmap.
            </p>
        </div>
        <form method="POST" action="/app/upload/summarise"
              data-loading="Generating summary..."
              data-overlay="AI is analysing your document and generating a strategic summary. This typically takes 15-30 seconds.">
            <input type="hidden" name="_csrf_token" value="<?= htmlspecialchars($csrf_token) ?>">
            <input type="hidden" name="document_id" value="<?= (int) $latestDoc['id'] ?>">
            <input type="hidden" name="project_id" value="<?= (int) $project['id'] ?>">
            <button type="submit" class="btn btn-ai">Generate AI Summary</button>
        </form>
    </div>
</section>
<?php endif; ?>

<!-- ===========================
     4. Summary Ready (below generate prompt)
     =========================== -->
<?php if ($hasSummary): ?>
<section class="card mb-6" style="border-left: 4px solid var(--success, #28a745);">
    <div class="card-body">
        <div style="display:flex; align-items:start; gap:1rem;">
            <div style="flex:1;">
                <h3 style="margin:0 0 0.5rem; color:var(--success, #28a745);">Summary Ready</h3>
                <p style="font-size:0.9rem; color:var(--text-secondary); line-height:1.6; margin:0;">
                    <?= htmlspecialchars(mb_substr($latestDoc['ai_summary'], 0, 400)) ?>
                    <?= mb_strlen($latestDoc['ai_summary']) > 400 ? '...' : '' ?>
                </p>
            </div>
        </div>
    </div>
</section>
<?php endif; ?>

<!-- ===========================
     5. Previous Documents (collapsed, always last)
     =========================== -->
<?php if ($hasDocuments): ?>
<section class="card">
    <div class="card-header flex justify-between items-center" style="cursor:pointer;"
         onclick="document.getElementById('doc-list').classList.toggle('hidden'); this.querySelector('.toggle-icon').textContent = document.getElementById('doc-list').classList.contains('hidden') ? '+' : '-';">
        <h2 class="card-title" style="margin:0;">
            Documents (<?= count($documents) ?>)
            <span class="toggle-icon" style="font-weight:400; margin-left:0.5rem;">+</span>
        </h2>
    </div>
    <div id="doc-list" class="hidden">
        <?php foreach ($documents as $doc): ?>
            <div style="border-bottom:1px solid var(--border);">
                <?php if (!empty($doc['ai_summary'])): ?>
                    <details style="margin:0;">
                        <summary style="padding:0.75rem 1.5rem; display:flex; align-items:center; justify-content:space-between; cursor:pointer; list-style:none; user-select:none;">
                            <div>
                                <strong style="font-size:0.9rem;"><?= htmlspecialchars($doc['original_name']) ?></strong>
                                <span class="text-muted" style="font-size:0.8rem; margin-left:0.5rem;">
                                    <?= formatFileSize((int) $doc['file_size']) ?> &middot; <?= date('j M Y', strtotime($doc['created_at'])) ?>
                                </span>
                            </div>
                            <span class="badge badge-success" style="font-size:0.7rem; flex-shrink:0;">
                                AI Summary &#9660;
                            </span>
                        </summary>
                        <div style="padding:0.75rem 1.5rem 1rem; background:#f8fafc; border-top:1px solid var(--border); font-size:0.875rem; line-height:1.6; white-space:pre-wrap; color:var(--text-secondary);">
                            <?= htmlspecialchars($doc['ai_summary']) ?>
                        </div>
                    </details>
                <?php else: ?>
                    <div style="padding:0.75rem 1.5rem; display:flex; align-items:center; justify-content:space-between;">
                        <div>
                            <strong style="font-size:0.9rem;"><?= htmlspecialchars($doc['original_name']) ?></strong>
                            <span class="text-muted" style="font-size:0.8rem; margin-left:0.5rem;">
                                <?= formatFileSize((int) $doc['file_size']) ?> &middot; <?= date('j M Y', strtotime($doc['created_at'])) ?>
                            </span>
                        </div>
                        <div>
                            <?php if (!empty($doc['extracted_text'])): ?>
                                <form method="POST" action="/app/upload/summarise" class="inline-form"
                                      data-loading="Summarising...">
                                    <input type="hidden" name="_csrf_token" value="<?= htmlspecialchars($csrf_token) ?>">
                                    <input type="hidden" name="document_id" value="<?= (int) $doc['id'] ?>">
                                    <input type="hidden" name="project_id" value="<?= (int) $project['id'] ?>">
                                    <button type="submit" class="btn btn-sm btn-ai" style="font-size:0.75rem;">Summarise</button>
                                </form>
                            <?php else: ?>
                                <span class="badge badge-secondary" style="font-size:0.7rem;">No text extracted</span>
                            <?php endif; ?>
                        </div>
                    </div>
                <?php endif; ?>
            </div>
        <?php endforeach; ?>
    </div>
</section>
<?php endif; ?>

<?php require __DIR__ . '/partials/workflow-nav.php'; ?>
```

- [ ] **Step 3: Verify syntax**

```bash
docker compose exec php php -l templates/upload.php
```

Expected: `No syntax errors detected`

- [ ] **Step 4: Commit**

```bash
git add templates/upload.php
git commit -m "feat(upload): reorder sections to upload → generate → summary flow, apply .btn-ai"
```

---

## Task 5: Prioritisation — Fibonacci WSJF dropdowns

**Files:**
- Modify: `templates/prioritisation.php`

The sticky Score column CSS was added in Task 1. This task replaces the score dropdown values for WSJF only.

- [ ] **Step 1: Read `templates/prioritisation.php`** around lines 148–157 to confirm the exact dropdown loop.

- [ ] **Step 2: Replace the score dropdown loop with a framework-conditional version**

Find (inside `foreach ($fields as $i => $field)` loop):
```php
                                <select class="score-dropdown" data-field="<?= $field ?>">
                                    <option value="0" <?= $vals[$i] === 0 ? 'selected' : '' ?>>-</option>
                                    <?php for ($n = 1; $n <= 10; $n++): ?>
                                        <option value="<?= $n ?>" <?= $vals[$i] === $n ? 'selected' : '' ?>><?= $n ?></option>
                                    <?php endfor; ?>
                                </select>
```

Replace with:
```php
                                <select class="score-dropdown" data-field="<?= $field ?>">
                                    <option value="0" <?= $vals[$i] === 0 ? 'selected' : '' ?>>-</option>
                                    <?php if ($isRice): ?>
                                        <?php for ($n = 1; $n <= 10; $n++): ?>
                                            <option value="<?= $n ?>" <?= $vals[$i] === $n ? 'selected' : '' ?>><?= $n ?></option>
                                        <?php endfor; ?>
                                    <?php else: ?>
                                        <?php foreach ([1, 2, 3, 5, 8, 13, 20] as $n): ?>
                                            <option value="<?= $n ?>" <?= $vals[$i] === $n ? 'selected' : '' ?>><?= $n ?></option>
                                        <?php endforeach; ?>
                                    <?php endif; ?>
                                </select>
```

- [ ] **Step 3: Verify syntax**

```bash
docker compose exec php php -l templates/prioritisation.php
```

Expected: `No syntax errors detected`

- [ ] **Step 4: Commit**

```bash
git add templates/prioritisation.php
git commit -m "feat(prioritisation): use SAFe Fibonacci scale (1,2,3,5,8,13,20) for WSJF dropdowns"
```

---

## Task 6: Risks — RPN before title, heatmap above list, deselection scroll fix

**Files:**
- Modify: `templates/partials/risk-row.php`
- Modify: `templates/risks.php`

### risk-row.php — RPN before title

- [ ] **Step 1: Read `templates/partials/risk-row.php`** to confirm the `.risk-info` and `.risk-scores` block structure.

- [ ] **Step 2: Move RPN badge from `.risk-scores` to inline before the title**

Find:
```php
    <div class="risk-info">
        <strong><?= htmlspecialchars($risk['title']) ?></strong>
        <?php if (!empty($risk['description'])): ?>
            <p class="risk-desc-preview"><?= htmlspecialchars(mb_substr($risk['description'], 0, 150)) ?><?= mb_strlen($risk['description']) > 150 ? '...' : '' ?></p>
        <?php endif; ?>
        <div class="risk-linked-items">
            <?php foreach ($risk['linked_items'] as $li): ?>
                <span class="badge badge-secondary"><?= htmlspecialchars($li['title']) ?></span>
            <?php endforeach; ?>
        </div>
    </div>
    <div class="risk-scores">
        <span class="risk-badge likelihood-<?= (int) $risk['likelihood'] ?>">L: <?= (int) $risk['likelihood'] ?></span>
        <span class="risk-badge impact-<?= (int) $risk['impact'] ?>">I: <?= (int) $risk['impact'] ?></span>
        <span class="risk-rpn">RPN: <?= $rpn ?></span>
    </div>
```

Replace with:
```php
    <div class="risk-info">
        <div style="display: flex; align-items: center; gap: 0.5rem; margin-bottom: 0.25rem;">
            <span class="risk-rpn">RPN: <?= $rpn ?></span>
            <strong><?= htmlspecialchars($risk['title']) ?></strong>
        </div>
        <?php if (!empty($risk['description'])): ?>
            <p class="risk-desc-preview"><?= htmlspecialchars(mb_substr($risk['description'], 0, 150)) ?><?= mb_strlen($risk['description']) > 150 ? '...' : '' ?></p>
        <?php endif; ?>
        <div class="risk-linked-items">
            <?php foreach ($risk['linked_items'] as $li): ?>
                <span class="badge badge-secondary"><?= htmlspecialchars($li['title']) ?></span>
            <?php endforeach; ?>
        </div>
    </div>
    <div class="risk-scores">
        <span class="risk-badge likelihood-<?= (int) $risk['likelihood'] ?>">L: <?= (int) $risk['likelihood'] ?></span>
        <span class="risk-badge impact-<?= (int) $risk['impact'] ?>">I: <?= (int) $risk['impact'] ?></span>
    </div>
```

### risks.php — heatmap above risk list

- [ ] **Step 3: Read `templates/risks.php`** to confirm the exact section boundaries for the heatmap card and risk list card.

- [ ] **Step 4: Move the entire heatmap section above the risk list**

The heatmap section starts at `<!-- 5x5 Heatmap -->` (the `<div class="card mb-6">` before the heatmap grid) and ends at its matching `</div>` before `<?php require __DIR__ . '/partials/workflow-nav.php'; ?>`.

Cut that entire block and paste it immediately after the closing `</div>` of the Add/Edit Risk Modal (the `<!-- Add/Edit Risk Modal -->` block) and before the `<!-- Risk List -->` block.

The new order in `risks.php` should be:
1. Page Header
2. Add/Edit Risk Modal
3. **Heatmap card** ← moved here
4. Risk List card
5. `workflow-nav` partial
6. `<script>` block
7. `<style>` block

### risks.php — clearHeatmapFilter deselection fix

- [ ] **Step 5: Add scroll-to-top in `clearHeatmapFilter()`**

Find:
```js
function clearHeatmapFilter() {
    heatmapFilter = { likelihood: null, impact: null };
    document.querySelectorAll('.heatmap-cell').forEach(function(cell) {
        cell.classList.remove('heatmap-cell--selected');
    });
    document.querySelectorAll('.risk-row').forEach(function(row) { row.style.display = ''; });
    var banner = document.getElementById('heatmap-filter-banner');
    if (banner) banner.remove();
}
```

Replace with:
```js
function clearHeatmapFilter() {
    heatmapFilter = { likelihood: null, impact: null };
    document.querySelectorAll('.heatmap-cell').forEach(function(cell) {
        cell.classList.remove('heatmap-cell--selected');
    });
    document.querySelectorAll('.risk-row').forEach(function(row) { row.style.display = ''; });
    var banner = document.getElementById('heatmap-filter-banner');
    if (banner) banner.remove();
    var riskList = document.querySelector('.risk-list');
    if (riskList) riskList.scrollIntoView({ behavior: 'smooth', block: 'start' });
}
```

- [ ] **Step 6: Verify syntax**

```bash
docker compose exec php php -l templates/risks.php
docker compose exec php php -l templates/partials/risk-row.php
```

Expected: `No syntax errors detected` for both.

- [ ] **Step 7: Commit**

```bash
git add templates/risks.php templates/partials/risk-row.php
git commit -m "feat(risks): RPN before title, heatmap above risk list, fix deselection scroll"
```

---

## Task 7: Final verification and push

- [ ] **Step 1: Run full unit test suite** (if Docker is running)

```bash
docker compose exec php vendor/bin/phpunit tests/Unit --no-coverage 2>&1 | tail -5
```

Expected: same pre-existing failures only — no new failures introduced.

- [ ] **Step 2: Smoke-test key screens**

1. **Any screen with AI buttons** — verify they render as bright blue (`#3b82f6`), visually distinct from the dark navy `#1d4ed8` primary buttons
2. **Upload** — upload a document, verify upload section is first; after extraction the "Generate AI Summary" button appears below the upload card (not above it); after summarising the summary appears below
3. **Work Items** — verify "Regenerate Work Items" and "Regenerate Sizing" are right-aligned; open Edit modal and verify it is noticeably wider
4. **Prioritisation (WSJF mode)** — verify dropdowns show `-, 1, 2, 3, 5, 8, 13, 20`; switch to RICE — verify `-, 1–10`; scroll the table right — verify Score column stays visible (sticky)
5. **Risks** — verify heatmap appears above the risk list; verify RPN badge appears before each risk title; click a heatmap cell then click it again — verify viewport scrolls back to risk list top and no row appears pinned

- [ ] **Step 3: Push**

```bash
git push
```
