[build]
dockerfilePath = "Dockerfile"
# Force no cache on rebuild
watchPatterns = ["**"]

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
