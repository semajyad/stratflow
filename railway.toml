[build]
builder = "NIXPACKS"

[deploy]
startCommand = "php scripts/init-db.php 2>&1 || true; php -S 0.0.0.0:${PORT:-8080} -t public public/router.php"
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
